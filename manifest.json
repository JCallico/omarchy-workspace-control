{
  "schemaVersion": 1,
  "id": "jcallico.workspace-control",
  "name": "Workspace Control",
  "version": "0.1.0",
  "author": "jcallico",
  "license": "MIT",
  "description": "A growing toolkit of workspace-related features for Omarchy. First feature: remembers exactly which apps are open on each workspace, at what position and size, and puts them back after a reboot or logout.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Workspace Control",
    "description": "Watches your Hyprland layout per workspace and restores it on login.",
    "category": "Productivity",
    "allowMultiple": false,
    "defaultSection": "left",
    "defaults": {
      "watchEnabled": true,
      "restoreOnLogin": true,
      "debounceSec": 2,
      "fallbackIntervalMin": 15,
      "excludeClasses": ""
    },
    "schema": [
      {
        "key": "watchEnabled",
        "type": "boolean",
        "label": "Watch for layout changes",
        "description": "Automatically re-save a workspace's layout a couple of seconds after you open, close, move, float, pin, or fullscreen a window on it.",
        "defaultValue": true
      },
      {
        "key": "restoreOnLogin",
        "type": "boolean",
        "label": "Restore on login",
        "description": "Relaunch anything missing from the last saved layout, on its saved workspace, once per login.",
        "defaultValue": true
      },
      {
        "key": "debounceSec",
        "type": "integer",
        "label": "Save debounce (seconds)",
        "description": "How long to wait after the last layout change on a workspace before saving it.",
        "min": 1,
        "max": 30,
        "step": 1,
        "defaultValue": 2
      },
      {
        "key": "fallbackIntervalMin",
        "type": "integer",
        "label": "Safety-net rescan (minutes)",
        "description": "Hyprland doesn't report interactive window drags/resizes as events, so this periodic full rescan is the only thing that catches floating-window geometry drift.",
        "min": 1,
        "max": 120,
        "step": 1,
        "defaultValue": 15
      },
      {
        "key": "excludeClasses",
        "type": "string",
        "label": "Excluded app classes",
        "description": "Comma-separated window classes to never save or restore (e.g. launcher popups, one-off dialogs).",
        "defaultValue": ""
      }
    ]
  }
}
